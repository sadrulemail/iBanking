<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tblbd.com.ibanking">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LoginActivityTheme">
        <activity android:name=".ActivityLogin2" />
        <activity android:name=".AccountList" />
        <activity android:name=".BillPay" />
        <activity android:name=".FundTransfer" />
        <activity android:name=".AccountStatement" />
        <activity android:name=".LinkAccount" />
        <activity android:name=".HomeDashboard" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ActivityLogin" />
    </application>

</manifest>